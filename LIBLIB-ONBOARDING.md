# LiblibAI 企业认证操作手册（阶段 0）

> 目标：用「陕西典致广告有限公司」资质完成 LiblibAI 企业实名认证 + 申请 API 密钥，拿到 AccessKey / SecretKey，填入 ai-workbench 即可用 Liblib 一个 Key 调聚合生图。
> 版本 v1 · 2026-09-03 · 旺财出品 · 入口名称随平台版本可能微调，以实际页面为准。

---

## 一、要准备的材料（先备齐再动手）

| 材料 | 说明 | 王哥现状 |
|---|---|---|
| **营业执照扫描件** | 清晰显示统一社会信用代码 | ✅ 有（91611104338609884P） |
| **法人身份证正反面** | 法人是叶荣，需找他拿 | ⚠️ 需找叶荣要 |
| **加盖公章的《企业商用授权承诺书》** | 平台下载模板（**商用才需要**） | ⚠️ 需盖章 |
| 对公/可开发票的信息 | 用于后续购买 API 积分、开发票 | ✅ 有 |

> 关键提醒：**法人是叶荣，不是王哥你**。企业认证绑定的是法人身份证，这步得提前跟叶荣打招呼拿证件照片。

---

## 二、六步操作（照着点）

### 第 1 步：登录并找入口
1. 浏览器打开 `https://www.liblib.art`，用手机号验证登录（或已有账号）
2. 左侧导航栏找 **「API 开发平台」/「开放平台」/「开发者中心」** 任一入口点进去
3. 若显示「暂未开放」或没有「创建密钥」按钮 → 说明还没完成企业认证，先走第 2 步

### 第 2 步：企业实名认证
1. 进入 **「个人中心」→「实名认证」**（或「账户中心」→「认证管理」）
2. 选 **「企业认证」**，上传营业执照 + 法人身份证正反面
3. 提交后**人工审核 1–3 个工作日**（个体工商户资质更快，约 2 小时）

### 第 3 步：领试用积分 / 买 API 积分
1. 认证通过后，进「API 开发平台」领 **500 试用积分（7 天免费测试）**
2. 注意：**API 积分与网页会员积分是两套体系**，API 积分要单独购买，支持对公打款 + 发票

### 第 4 步：创建 API 密钥
1. 进「开发者中心」→「API 管理」→「新建 API 密钥 / 创建密钥」
2. 填**应用名称**（如「典致内容工作台」）+ **用途说明**（如「用于电商主图与广告设计内容配图」）
3. 提交后立即显示 **AccessKey + SecretKey**
4. ⚠️ **SecretKey 只显示这一次**，关闭页面就永久丢失，必须立刻复制保存（先存备忘录，再填进工作台）

### 第 5 步：企业商用授权（你的广告/电商图都要商用，必做）
1. 进「商用授权」→「申请企业商用」
2. 上传营业执照 + 法人身份证 + 加盖公章的《企业商用授权承诺书》
3. 填**实际使用场景**：如「电商主图制作」「线下展会海报设计」「标识物效果图」
4. 审核 1–3 个工作日，通过后账户显示「企业商用已启用」

### 第 6 步：填进 ai-workbench 设置页
打开工作台 → 设置 → 找到 **「LiblibAI（聚合出图 · 双KEY · 企业认证）」** 分组，照下表填：

| 设置页字段 | 填什么 |
|---|---|
| AccessKey | 第 4 步拿到的 AccessKey |
| SecretKey | 第 4 步拿到的 SecretKey |
| Base URL | 保持默认 `https://openapi.liblibai.cloud`（不用改） |
| 模型名 | 留空用默认，或填平台模型名（如 `Seedream 4.0`） |

填完点 **「测试连接」**，显示「连通正常，双KEY签名鉴权通过」即成功。

---

## 三、拿到 Key 后回到我这

把 AccessKey / SecretKey 给我（或自己填进设置页后告诉我），我接着做：

1. **实测校对**：按开发者中心官方文档，校验签名算法（HMAC-SHA1 + 三参数）、文生图接口路径、批量张数字段名、轮询字段名，把阶段 1 适配器调准
2. **跑第一张真实图**：用木盆产品图走一遍「需求 → 提示词 → Liblib 出图 → 落素材库」，验证端到端
3. **推进阶段 2**：风格模型库页（搜/收藏/套用电商 LoRA）

---

## 四、红线提醒（别踩）

1. **个人免费账号没有 API Key**——必须企业认证，别拿个人号白折腾
2. **两套 API 别搞混**：`openapi.liblibai.cloud`（AccessKey+SecretKey，SD 模型/工作流，我已按这套写适配器）≠ `api.liblib.art`（Bearer key，聚合官方模型）
3. **SecretKey 只显示一次**，丢失只能重新生成
4. **商用图默认带水印**，除非买「去水印商用许可」；出商业图前逐个看模型「可商用」授权标签
5. **提示词别碰第三方版权内容**（如米老鼠、宝可梦），否则商用授权会被冻结
